# 机械臂上位机功能规划

> 基于当前 PlasmaRobot 系统能力梳理
> 机械臂: RM_ECO65 | 相机: RealSense (640×480) | ROS2: galactic

---

## 一、建议的右侧机械臂控制面板功能

### 1. 状态监控区（实时显示）

```
┌─────────────────────────────────┐
│ 🔵 机械臂状态                     │
│─────────────────────────────────│
│ 状态: 🟢 空闲 / 🟡 运行 / 🔴 错误  │
│ 连接: ✅ IP (192.168.1.18)       │
│─────────────────────────────────│
│ 关节角度                          │
│  J1: -0.20°    J2: 0.97°        │
│  J3: -0.95°    J4: -1.63°       │
│  J5: 1.41°     J6: -1.51°       │
│─────────────────────────────────│
│ 关节温度              │ 关节电流  │
│  J1: 25.0°C          │ 72mA     │
│  J2: 25.0°C          │ 894mA    │
│  ...                 │ ...      │
│─────────────────────────────────│
│ 末端位姿 (TCP)                     │
│  x: 0.352  y: -0.124  z: 0.518  │
│  rx: 3.14  ry: 0.00  rz: 0.00   │
│─────────────────────────────────│
│ 当前末端: 🔧 按实际情况显示         │
│ TCP 偏移: [0, 0, 0.108]          │
│ 碰撞检测: 🟢 安全                  │
└─────────────────────────────────┘
```

**数据来源**: `/joint_states` (sensor_msgs/JointState) + `/arm_self_check_status` (std_msgs/String, 2Hz JSON)

**自检服务**: `/arm_self_check` (std_srvs/Trigger) — 调用后返回完整自检报告

---

### 2. 运动控制区

```
┌─────────────────────────────────┐
│ 🎮 运动控制                       │
│─────────────────────────────────│
│ 关节点动:                         │
│  ┌──── J1 ────┐  ┌──── J2 ────┐│
│  │  ◄     ►   │  │  ◄     ►   ││
│  └────────────┘  └────────────┘│
│  ┌──── J3 ────┐  ┌──── J4 ────┐│
│  │  ◄     ►   │  │  ◄     ►   ││
│  └────────────┘  └────────────┘│
│  ┌──── J5 ────┐  ┌──── J6 ────┐│
│  │  ◄     ►   │  │  ◄     ►   ││
│  └────────────┘  └────────────┘│
│─────────────────────────────────│
│ 速度: [═══●═══] 30%             │
│─────────────────────────────────│
│ [🏠 Home]  [↩ Zero]  [🔄 Forward] │
│ [🔓 释放刹车]  [🛑 急停]          │
└─────────────────────────────────┘
```

**对应脚本**: `scripts/pointcloud_get/robot_control/release_brake.py`, `move_to_point.py`

---

### 3. 相机与采集区

```
┌─────────────────────────────────┐
│ 📷 相机控制                       │
│─────────────────────────────────│
│ 状态: ✅ 已连接 (640×480@30fps)   │
│─────────────────────────────────│
│ [📸 RGB画面]  [🌌 Depth画面]      │
│ [☁️ 点云预览]                     │
│─────────────────────────────────│
│ 采集参数:                         │
│  帧数: [5]                       │
│  稳定时间: [2.0] 秒              │
│  步长(stride): [1]               │
│─────────────────────────────────│
│ [📷 单帧采集]  [🎬 序列采集]       │
│─────────────────────────────────│
│ 上次采集: 2026-06-28 14:30       │
│ 点数: 184,320                    │
│ 保存路径: calib_data/<实验名>/    │
└─────────────────────────────────┘
```

**对应脚本**: `scripts/pointcloud_get/capture/collect_model_rgbd_pcd_l515_rm.py`

**启动命令示例**:
```bash
python3 capture/collect_model_rgbd_pcd_l515_rm.py \
    --handeye_yaml calib_data/handeye_matrices/current/camera_to_gripper.yaml \
    --out_dir calib_data/<实验名> \
    --stride 1
```

---

### 4. 点云处理区

```
┌─────────────────────────────────┐
│ ✂️ 点云处理                       │
│─────────────────────────────────│
│ 裁剪区域 (CropBox):              │
│  x: [ -0.5 ████████░░░ 0.5 ]   │
│  y: [ -0.5 ████████░░░ 0.5 ]   │
│  z: [  0.0 ████████░░░ 0.6 ]   │
│─────────────────────────────────│
│ [🔍 选择对象] → 框选感兴趣区域     │
│─────────────────────────────────│
│ 凹坑分析结果:                      │
│  ─ 拟合轴线: (0.12, 0.98, 0.12) │
│  ─ 法向量:   (0.01, 0.99, 0.08) │
│  ─ 置信度:   87.3%               │
│  ─ 凹坑深度:  2.4 mm             │
│  ─ 凹坑直径:  8.1 mm             │
│─────────────────────────────────│
│ [📐 重新计算轴线]  [✅ 确认轴线]    │
│ [💾 保存目标点]                    │
└─────────────────────────────────┘
```

**保存格式** (target_point.yaml):
```yaml
target_point:
  position: [0.352, -0.124, 0.518]
  approach_axis: [0.12, 0.98, 0.12]
  normal: [0.01, 0.99, 0.08]
```

---

### 5. 喷涂执行区

```
┌─────────────────────────────────┐
│ 🔫 喷涂执行                       │
│─────────────────────────────────│
│ 当前末端: 🔧 石英管 6_8/200_1x10 │
│ TCP: [0, 0, 0.196]               │
│─────────────────────────────────│
│ 执行参数:                         │
│  接近距离: [  80.0  ] mm         │
│  下降步长: [   5.0  ] mm         │
│  碰撞阈值: [ 1500.0 ] mA         │
│  工作空间边界:                     │
│    x: [-0.70, 0.70]              │
│    y: [-0.60, 0.60]              │
│    z: [ 0.10, 0.60]              │
│─────────────────────────────────│
│ 执行进度:                         │
│  □ 阶段1/4: 移动到接近点   ✅      │
│  □ 阶段2/4: 沿轴下降      ▶️      │
│  □ 阶段3/4: 喷涂中         ⏳      │
│  □ 阶段4/4: 退回安全位置   ⏳      │
│─────────────────────────────────│
│ [▶️ 开始执行]  [🛑 紧急停止]       │
│ [🔁 重试]     [✏️ 修改参数]        │
└─────────────────────────────────┘
```

**对应主力脚本**: `scripts/pointcloud_get/robot_control/plan_and_move.py`

**工作流程**:
```
Phase 1: MoveIt 规划到接近点(10cm上方) + 笛卡尔直线下降
Phase 2: 路径预览 → Rviz 确认
Phase 3: SDK 执行 MoveIt 规划的关节轨迹
         ├─ 碰撞检测 8 级
         ├─ 关节空间 → 接近点
         ├─ 悬停 → 目视确认
         └─ 分段下降(~5mm/步, 电流增量检测)
```

---

### 6. 底部状态栏

```
┌─────────────────────────────────────────────────────────┐
│ 📋 日志消息              │ 💡 系统: 🟢 正常  │ 📍 工作模式  │
│                          │ 上次错误: 无       │   标定/喷涂  │
└─────────────────────────────────────────────────────────┘
```

---

## 二、数据接口清单

所有 ROS2 接口已由现有 `plasma_robot/` 包提供：

| 接口 | 类型 | 用途 |
|------|------|------|
| `/joint_states` | Topic (sensor_msgs/JointState) | 关节角度/速度/力矩 |
| `/arm_self_check_status` | Topic (std_msgs/String, 2Hz) | 自检状态 JSON |
| `/arm_self_check` | Service (std_srvs/Trigger) | 触发自检 |
| `/camera/color/image_raw` | Topic (sensor_msgs/Image) | RGB 画面 |
| `/camera/depth/image_rect_raw` | Topic (sensor_msgs/Image) | 深度图 |
| `/camera/depth/color/points` | Topic (sensor_msgs/PointCloud2) | 点云 |
| `/move_group` | Action (moveit_msgs/MoveGroup) | 运动规划 |

---

## 三、开发优先级建议

| 优先级 | 功能 | 理由 |
|--------|------|------|
| ⭐⭐⭐ | **状态监控**（关节/温度/错误码） | 必须知道机械臂是否正常 |
| ⭐⭐⭐ | **急停按钮** | 安全第一 |
| ⭐⭐⭐ | **相机画面 + 采集触发** | 核心工作流第一步 |
| ⭐⭐⭐ | **末端选择**（tool/printed-part） | 贯穿所有操作 |
| ⭐⭐ | **点云显示 + 对象选择** | 核心工作流第二步 |
| ⭐⭐ | **自动轴线计算 + 显示** | 核心工作流第三步 |
| ⭐⭐⭐ | **喷涂执行 + 进度** | 核心工作流第四步 |
| ⭐ | **关节点动** | 调试用，非必须 |

---

## 四、启动系统速查

```bash
# 裸臂 + 相机
./start_arm.sh

# 3D打印笔尖标定模式
./start_arm.sh --no-camera --printed-part

# 等离子喷杆工作模式
./start_arm.sh --no-camera --tool 3_4/3_4_150_1x5

# 带 HTTP API 的工作模式
./start_arm.sh --work-mode

# 查看所有参数
./start_arm.sh --help
```

手眼矩阵路径:
```
calib_data/handeye_matrices/current/camera_to_gripper.yaml
→ 当前指向: auto_tsai_20260624_211307
```
